#include <iostream>

int main()
{
    for (int i = 0, i < s.size(); ++i)
    {
        std::cout << s[i];
        std::cout << '\n';
    }

    for (int i = 0; i < s.size(); ++i)
        std::cout << s[i];
    std::cout << '\n';
}
